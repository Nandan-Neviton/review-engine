# Coding Rules

- Avoid console.log in production code
- Avoid TODO/FIXME comments in final commits
- Sensitive values must never be hardcoded
- Auth modules require careful validation
- Large change sets should be minimized